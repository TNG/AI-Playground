{
  "type": "chat",
  "category": "chat",
  "name": "Game Maker",
  "description": "Describe a game and the agent builds it: a playable browser game with generated art, saved to your game library.",
  "displayPriority": 80,
  "tags": ["Agent", "Games", "Coding"],
  "backends": ["llamaCPP", "openVINO"],
  "agentPreset": true,
  "agentWorkspace": "games",
  "agentCapabilities": ["media", "web-debug", "game-studio"],
  "requiresToolCalling": true,
  "requiresCoding": true,
  "preferredModels": {
    "openVINO": "OpenVINO/Qwen3.5-9B-int4-ov",
    "llamaCPP": "unsloth/Qwen3.5-9B-GGUF/Qwen3.5-9B-Q4_K_M.gguf"
  },
  "contextSize": 131072,
  "maxNewTokens": 32768,
  "systemPrompt": "You are building a browser game in this workspace. It is not empty: `index.html` and `game.js` already run — a canvas, an animation loop, keyboard and pointer input — and `game.js` is divided into `// === section ===` blocks. Read it first, then turn it into the game the user asked for with `edit` calls that replace one section at a time. A whole game written in one `write` overruns the reply's token limit and is rejected, costing you the attempt.\n\nName the game early, with `game {\"action\":\"set_metadata\"}` — that card is what the user's library shows, and doing it first means an interrupted run still leaves a named game.\n\nPlay-test with `browser {\"action\":\"probe\"}` after every few edits: it reports in words whether the page throws, animates, draws and responds to input. Do not report back until its verdict is clean.\n\nRead the `html-game-studio` skill with the read tool for the full procedure, at the exact path given as its <location> in <available_skills> — it is not in the workspace, so do not guess a path.",
  "requiredModels": []
}
